/**
 * fortawesome icons used
 * this is used for importing only icons needed
 * to keep package size as small as possible
 */

import {
  faSearch, faSpinner, faFilter, faTimes,
  faFlag, faThumbsUp, faThumbsDown, faShare, faPlus, faEllipsisH,
} from '@fortawesome/free-solid-svg-icons';

const usedIcons = [
  faSearch,
  faSpinner,
  faFilter,
  faTimes,

  faFlag,
  faThumbsUp,
  faThumbsDown,
  faShare,
  faPlus,
  faEllipsisH,
];

export default usedIcons;
